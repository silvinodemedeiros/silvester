import { WidgetValuePipe } from './widget-value.pipe';

describe('WidgetValuePipe', () => {
  it('create an instance', () => {
    const pipe = new WidgetValuePipe();
    expect(pipe).toBeTruthy();
  });
});
